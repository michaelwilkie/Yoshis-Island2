/*#include "Particle.h"

Particle::Particle(int x, int y)
{
	//Set offsets
	mPosX = x - 5 + (rand() % 25);
	mPosY = y - 5 + (rand() % 25);
}
void Particle::render()
{
	//Show image
	mTexture->render(mPosX, mPosY);

	//Show shimmer
	
	//Animate
	mFrame++;
}
bool Particle::isDead()
{
	return mFrame > 10;
}*/